services:
  recorder:
    build:
      context: ./recorder
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      RTSP_URL: ${RTSP_URL:?set RTSP_URL in .env}
      DATA_DIR: /data
      SEGMENT_SECONDS: ${SEGMENT_SECONDS:-300}
      RETENTION_DAYS: ${RETENTION_DAYS:-14}
    volumes:
      - replay_data:/data

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${API_PORT:-8000}:8000"
    environment:
      DATABASE_URL: sqlite:////data/app.db
      DATA_DIR: /data
      SECRET_KEY: ${SECRET_KEY:-dev-secret-change-me}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-changeme}
      DOWNLOAD_TOKEN_EXPIRE_SECONDS: ${DOWNLOAD_TOKEN_EXPIRE_SECONDS:-86400}
      PAYMENT_PROVIDER: ${PAYMENT_PROVIDER:-manual}
      PAYMENT_WEBHOOK_SECRET: ${PAYMENT_WEBHOOK_SECRET:-webhook-secret-change}
      BUSINESS_MODE: ${BUSINESS_MODE:-custom}
      REPLAY_TRIGGER_WINDOW_SECONDS: ${REPLAY_TRIGGER_WINDOW_SECONDS:-30}
      REPLAY_HOOK_SECRET: ${REPLAY_HOOK_SECRET:-dev-replay-secret-change-me}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-}
    volumes:
      - replay_data:/data

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    command: ["python", "-m", "app.worker_main"]
    environment:
      DATABASE_URL: sqlite:////data/app.db
      DATA_DIR: /data
      SEGMENT_SECONDS: ${SEGMENT_SECONDS:-300}
    volumes:
      - replay_data:/data

  # USB Serial → POST /hooks/replay-trigger (perfil bridge). Ative com COMPOSE_PROFILES=bridge no .env ou: docker compose --profile bridge up
  replay-bridge:
    profiles:
      - bridge
    build:
      context: ./scripts
      dockerfile: Dockerfile.replay-bridge
    restart: unless-stopped
    depends_on:
      - api
    environment:
      SERIAL_PORT: /dev/ttyACM0
      SERIAL_BAUD: ${SERIAL_BAUD:-115200}
      REPLAY_URL: http://api:8000/hooks/replay-trigger
      REPLAY_HOOK_SECRET: ${REPLAY_HOOK_SECRET:-dev-replay-secret-change-me}
      REPLAY_SERIAL_COMMAND: ${REPLAY_SERIAL_COMMAND:-REPLAY_CUT}
    devices:
      - ${SERIAL_DEVICE:-/dev/ttyACM0}:/dev/ttyACM0

volumes:
  replay_data:
