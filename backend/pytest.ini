[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    unit: testes unitários rápidos sem ffmpeg
    integration: testes de integração que invocam ffmpeg/ffprobe reais
